# frozen_string_literal: true

require "bundler/gem_tasks"

# Author-side companion checks from rails-hyperdrive:
#   rake hyperdrive:manifest:check  — strict lint of hyperdrive.yml
#   rake hyperdrive:skills:check    — template/static drift (no-op: no templates)
require "hyperdrive/skill_tasks"
